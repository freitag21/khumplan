# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริงจากโปรเจค Supabase
# (Project Settings > API)
#
# ถ้าไม่ใส่ค่า แอปจะทำงานแบบ "ออฟไลน์" ได้ (คำนวณ Protection Gap ในเครื่อง)
# แต่จะบันทึก/แชร์ผลลัพธ์ และล็อกอินไม่ได้

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
