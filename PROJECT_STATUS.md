# KhumPlan — สถานะโปรเจค

อัปเดต: 2026-08-28 · **สถานะ: เปิดใช้งานจริงแล้ว (LIVE) — https://khumplan.com** · landing page + สมุดลูกค้า (Module B) ขึ้นแล้ว

## ตัดสินใจแล้ว

- แยกขาดจาก ChanSpace
- กลุ่มประกัน: ชีวิต, สุขภาพ, สะสมทรัพย์ (+ ยูนิตลิงก์)
- Module A: ตัวแทนกรอกแบบสอบถามเอง
- Stack: Vite vanilla + Supabase + **Vercel**
- ดีไซน์: ระบบ "Nocturne" (light palette) จาก Claude Design — `design/DESIGN_NOTES.md`
- ชื่อ: **คุ้มแพลน / KhumPlan** (เคาะแล้ว) · โดเมน **khumplan.com** จดที่ Cloudflare Registrar · `BRAND` ใน `src/ui/dom.js`

## ทำเสร็จแล้ว

### เครื่องมือ + ดีไซน์
- engine 6 หมวด + โมดูลภาษี + โมดูลแนะนำ + MANHA
- ดีไซน์ Nocturne ใช้จริง: หน้าผล (ทิศทาง A) · ฟอร์ม stepper 6 ขั้น · MANHA · Landing · shell/routing
- **Auth อีเมล + รหัสผ่าน** — สมัคร/เข้าสู่ระบบ/ลืมรหัสผ่าน/ตั้งรหัสใหม่ (`src/auth.js`, `renderAuth`)
- **แดชบอร์ดตัวแทน** (`src/ui/dashboard.js`, `?view=dashboard`): รายการผลวิเคราะห์ (ค้นหา/เปิด/แก้ไข/ลบ) + สถิติรายเดือน + แก้โปรไฟล์ (ชื่อ/LINE/บริษัท/เลขใบอนุญาต) · เปิดผลเดิมมาแก้แล้วบันทึกทับ (`?edit=<id>`)
- Supabase: schema + RLS, บันทึก + ลิงก์แชร์ (?a=slug) + migration `0002` (trigger เก็บ display_name) · **เชื่อมโปรเจคจริง + ทดสอบ end-to-end ผ่าน** (2026-08-28)
- **Financial Pyramid** (จากทีมตัวแทน) — ปิรามิด 3 ชั้นในหน้าผล สีตาม coverage + marker "คุณอยู่ตรงนี้" (`src/lib/pyramid.js`, `src/ui/pyramid.js`)
- **Dark mode** — palette มืด + ปุ่มสลับใน topbar + จำค่า localStorage + พิมพ์บังคับโหมดสว่าง
- เทสต์ engine 26 เคส (vitest) · `npm run build` ผ่าน · ตรวจใน Chrome (light + dark) ไม่มี error

### รอบแก้ตามรีวิวของ Win (2026-08-28)
**บั๊ก P0 แก้แล้ว:**
- ลูกค้าโสดไม่มีผู้พึ่งพา → ทดแทนรายได้ = 0, label "คุ้มครองหนี้สิน" (เดิมได้ทุน 12 ล้าน)
- ช่องว่างรวมแยกเป็น 2 ก้อน: "ทุนคุ้มครองที่ยังขาด" (ชีวิต+โรคร้าย) กับ "เป้าหมายเงินออม" (เกษียณ+การศึกษา) — ไม่รวมค่าห้อง(บาท/วัน) และ PA(นับซ้ำ)
- หน้าผลนำด้วย **"ก้าวแรก — เริ่มที่ [ด้านเร่งสุด]"** ไม่ใช่ตัวเลขรวมหลักสิบล้าน
- เกษียณเลยอายุแล้ว → โหมด decumulation (เงินใช้ได้กี่ปี) แทนหารด้วยศูนย์
- MANHA ฟอร์มเปล่า → ปุ่มประเมิน disabled จนกรอกครบ
- ภาษี: คำนวณ ภาษีก่อน−หลัง + ค่าลดหย่อนคู่สมรส/บุตร/บิดามารดา/ปกส./ดอกเบี้ยบ้าน + บังคับเพดานรวม 500,000 + ใช้ healthSubLimit + ช่องกรอกเงินได้สุทธิตรง ๆ + ระบุปีภาษี
- การศึกษา: คำนวณเงินออม/เดือนแยกรายคนแล้วรวม + เงินที่มีทบต้น + ค่าเทอม K‑12 แสดงแยกเป็นกระแสเงินสด
- RLS หน้าแชร์: เปลี่ยนเป็น RPC `get_shared_analysis(slug)` + ลิงก์หมดอายุ 90 วัน + ต้องยืนยัน consent ก่อนบันทึก
- MANHA หน้าจอ: มีบรรทัด PDPA (ข้อมูลอยู่ในเครื่อง ยังไม่บันทึก)

**สมมติฐานปรับแล้ว (`assumptions.js`):**
- medical inflation 7% → 9% · life expectancy M80/F85 → M85/F90 · post-retire return 2% → 3%
- pre-retire return แยกตามเวลาที่เหลือ (5% ถ้า >15 ปี, 4% ถ้าน้อยกว่า)
- ทุนชีวิตคิด present value (real return) + เพดาน 15 เท่าของรายได้/ปี + กันเงินสำรองฉุกเฉิน 6 เดือนก่อนหักสภาพคล่อง
- เกษียณใช้ real-return annuity factor (เลิก drawdownAdjustment 0.85) + รวม PVD ต่อเนื่อง + หักบำนาญ ปกส.
- CI ผูกกับวงเงินสุขภาพที่มี (ทดแทนรายได้ 30 เดือน + top-up ตามชั้น) แทนตัวคูณ 3 เท่ารายได้
- PA `excludeFromTotal` — เป็นส่วนเสริม ไม่นับซ้ำทุนชีวิต
- วงเงินเหมาจ่ายสุขภาพเป้าหมาย: รัฐ 1M / เอกชนทั่วไป 5M / พรีเมียม 25M
- หักสิทธิรัฐ (ปกส./บัตรทอง/ข้าราชการ) ในหมวดสุขภาพ
- finalExpenses 200k → 300k

**MANHA เปลี่ยนเป็นโมเดล "ขั้นถัดไป":**
- เลิก go/no-go — Need/Money "ไม่พร้อม" ไม่ใช่เหตุให้ทิ้ง แต่เป็น "นัดเพื่อค้นหาความต้องการ" / "เริ่มแผนเล็ก + ติดตาม"
- STOP เหลือเฉพาะเชิงเทคนิค (อยู่ระหว่างรักษา / เคยถูกปฏิเสธ)
- age < 20 = ผู้เยาว์ (กฎหมายไทย) · age > 75 = watch ไม่ใช่ stop
- เพิ่มช่องงบเบี้ย + บล็อก "การเข้าถึง" (ที่มาของรายชื่อ / มีตัวแทนอยู่แล้ว / โอกาสแนะนำต่อ) → จัดคิว
- readout มีโน้ต "ตั้งเตือนติดตาม 3/6/12 เดือน"

**ฟิลด์ใหม่ในแบบสอบถาม:** ผู้อยู่ในอุปการะ (คู่สมรส/บิดามารดา) · รายได้คู่สมรส · ประกันกลุ่มจากที่ทำงาน (แยกจากส่วนตัว + แสดง "ถ้าออกจากงาน") · สิทธิรักษารัฐ · PVD/กบข. ต่อเดือน · บำนาญ ปกส. · RMF/SSF/PVD ต่อปี · งบเบี้ย · copay · เจ้าของกิจการ + หนี้ค้ำประกันส่วนตัว · สูบบุหรี่ · สถานะครอบครัวขยาย (โสด/สมรส/หย่า/หม้าย/เลี้ยงเดี่ยว) · แผง "ปรับสมมติฐาน" รายเคส · ระดับความเสี่ยง (สำหรับยูนิตลิงก์)

**recommend.js:** ยูนิตลิงก์เสนอเฉพาะเมื่อ riskTolerance รับได้ · ไม่สร้างรายการใหม่เพื่อสิทธิภาษี · มี budget note

**หน้าผล:** คะแนนเป็นระดับคำ (เริ่มต้น/กำลังสร้าง/ค่อนข้างครบ/ครบถ้วน) + coverage ถ่วงน้ำหนัก · การ์ด "จุดที่ทำไว้ดีแล้ว" (เขียว) · แบดจ์มีเลขใบอนุญาต · disclaimer + สมมติฐานที่ปรับพิมพ์ลงท้ายรายงาน

### รอบล่าสุด (2026-08-27) — deploy + launch
- แก้ layout พิมพ์ A4 พัง (Chrome ยุบ grid `1fr` ตอนพิมพ์ → flexbox) — พอดี 1 หน้า
- เตือนเมื่อเปิดหน้าผลใน in-app browser ของ LINE (ปุ่มพิมพ์มักไม่ทำงานบน Android)
- หน้า **วิธีใช้** · **ติดต่อเรา** · **ข้อกำหนดการใช้งาน** · **นโยบายความเป็นส่วนตัว (PDPA)** + config `CONTACT`
- หน้าแรก (root) แสดงหน้า Log in เมื่อยังไม่ล็อกอิน
- บังคับติ๊กยอมรับ Policy ตอนสมัคร + เก็บ `policy_accepted_at`/`_version` ใน user metadata
- แก้โลโก้หายในหน้า auth บนมือถือ
- **Deploy จริง**: GitHub `freitag21/khumplan` → Vercel (team AI SmartWork / Hobby, auto-deploy) → `khumplan.com` + `www` redirect + HTTPS (DNS ที่ Cloudflare, CNAME flattening ที่ apex)
- Supabase Auth URL Config อัปเดต (Site URL + Redirect URLs) · **"Confirm email" ปิดชั่วคราว** ให้ตัวแทนสมัครได้ทันที
- ดู `DEPLOYMENT_LOG.md` สำหรับรายละเอียดทั้งหมด

## ทิศทางธุรกิจ (ดู `STRATEGY.md` — memo การตลาดฉบับเต็ม)

**เดิมพันเดียว:** ทำให้หัวหน้าทีม 1 คน (ไม่ใช่ทีมผู้ก่อตั้ง) + ลูกทีม 10–20 คน จ่ายรายปี + บอกต่อ ภายใน 90 วัน
**โมเดล:** ฟรีตลอด (Module A) → **Team tier ขายหัวหน้าทีม** เป็นแกนรายได้ (Founding ฿9,900/ปี ถึง 10 ที่นั่ง ล็อก 2 ปี) · Pro รายบุคคล = โรงงาน testimonial ไม่ใช่แกน
**รายได้แรก:** ดีล Team รายปี ขายด้วยมือ + ใบแจ้งหนี้ PromptPay — ยังไม่สร้าง billing engine จนกว่าจะเกิน 50 ทีม

## Q ถัดไป — build แค่ 3 อย่าง (ห้ามแตะอย่างอื่น)

1. **Module B core** — สมุดลูกค้า: กรมธรรม์ที่ถืออยู่ + ข้อยกเว้น · เตือนต่ออายุ/วันเกิด เข้า LINE · ธง resale อัตโนมัติจาก Protection Gap · nurture list จาก MANHA
   - ✅ **สมุดลูกค้า + กรมธรรม์ที่ถืออยู่ + ข้อยกเว้น** (LIVE 2026-08-28, `?view=clients`, migration 0004) + ผูกผลวิเคราะห์เข้าลูกค้า
   - ✅ **งานติดตาม** (LIVE 2026-08-28, `?view=followups`, migration 0005): ต่ออายุ 90 วัน · วันเกิดเดือนนี้ · **ธง resale อัตโนมัติ** (หมวดช่องว่าง × ไม่มีกรมธรรม์รองรับ) · **nurture list จาก MANHA** (readout ไม่ใช่ "go" → บันทึกผู้มุ่งหวัง + ตั้งเตือน 3/6/12 เดือน) · reminder เพิ่มเอง/เช็ก done · ปุ่มวิเคราะห์จากหน้าลูกค้า prefill + auto-link
   - ⏳ เตือน LINE (ต้องตั้ง LINE Messaging API bot — ทำทีหลัง)
2. **แดชบอร์ดหัวหน้าทีม v1** — รายชื่อที่นั่ง, active ล่าสุด, จำนวนเคส, ยอดรวมทีม — **ข้อมูลรวม/กิจกรรมเท่านั้น ห้ามเห็นชื่อลูกค้าของลูกทีม (PDPA)**
3. **Team workspace + seat invite + Team trial 30 วัน**

พร้อมกัน: ✅ public landing page (LIVE — root = landing เมื่อยังไม่ล็อกอิน), ปุ่ม "เชิญทีม", โน้ต paid-roadmap บน landing/onboarding

## ยังไม่ได้ทำ / เปิดไว้ทีหลัง

- **ทบทวนสมมติฐานกับกลุ่มตัวแทนอีกรอบ** — ตัวเลขจาก Win คนเดียว
- นโยบาย "ตัวแทนออกจากทีม → export/downgrade สมุดลูกค้า" (landmine จริยธรรม — ทำก่อนขาย Team)
- ทนายตรวจ Terms/Privacy ก่อนเริ่มเก็บเงิน
- แปลง `public/og.svg` → `og.png` · redirect www 308 · assumptions review
- **Module C (data pool) — ห้ามแตะ 12+ เดือน** (ทำให้ตัวแทนระแวง + กิน bandwidth)
- White-label เอเจนซี / certification — หลังมี Team customer 2–3 ราย

## รีวิวของ Win — ข้อที่ยังค้าง (ทำต่อได้)

- โหมด "แผนที่จ่ายไหว" (กรอกงบ → จัดลำดับซื้ออะไรก่อน ปิดได้กี่ %)
- โหมดเปรียบเทียบ "ถ้าเกิดเหตุวันนี้" (ครอบครัวเหลือเงินใช้กี่เดือน)
- "โหมดนำเสนอ" 1 หน้าจอสำหรับโชว์บนมือถือ (→ ย้ายไป Pro tier)
- เตือนติดตาม (nurture list จาก MANHA) — อยู่ใน Module B core แล้ว

## รีวิวของ Win — ข้อที่ยังค้าง (ทำต่อได้)

- โหมด "แผนที่จ่ายไหว" (กรอกงบ → จัดลำดับซื้ออะไรก่อน ปิดได้กี่ %)
- โหมดเปรียบเทียบ "ถ้าเกิดเหตุวันนี้" (ครอบครัวเหลือเงินใช้กี่เดือน)
- "โหมดนำเสนอ" 1 หน้าจอสำหรับโชว์บนมือถือ
- เตือนติดตาม (nurture list จาก MANHA) — รอ Module B
